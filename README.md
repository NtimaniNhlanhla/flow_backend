# Getting Started with Flow Backend

## Run Backend project
 run the below command to install NPM packages:

### `npm install`

To start up a development instance, run the below command and the project will run on port 5000

### `npm run dev`


